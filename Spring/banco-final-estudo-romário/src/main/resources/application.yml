server:
  port: 8082

spring:
  application:
    name: Banco

  main:
    allow-circular-references: true

  datasource:
    url: jdbc:mysql://localhost:3306/db_banco?createDatabaseIfNotExist=true
    username: root
    password: ""

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
